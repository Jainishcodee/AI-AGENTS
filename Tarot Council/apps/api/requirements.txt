fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
httpx==0.28.1
Jinja2==3.1.5
PyYAML==6.0.2

# optional — audio for `app.cli brief --speak`. Free: a public Microsoft endpoint, no key.
# Left out of the pinned set on purpose: it is the only dependency that talks to a service
# nothing else needs, and `brief` produces its script without it (ADR-028).
#   pip install edge-tts

# dev
pytest==8.3.4
pytest-asyncio==0.25.2
