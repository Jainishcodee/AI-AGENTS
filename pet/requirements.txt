# Phase 1 — the pet itself
PySide6>=6.6
psutil>=5.9
pyttsx3>=2.90
python-dotenv>=1.0

# Phase 2 — voice + local brain (uncomment when you get there)
# SpeechRecognition>=3.10
# ollama>=0.3

# Phase 3 — task automation
# playwright>=1.40      # then: playwright install chromium
