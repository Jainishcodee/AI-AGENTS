Cover images for reel cards.

Generated by reelflow/media.py:
  python media.py --cookies brave

Instagram requires a logged-in session since mid-2026, so close the
browser first (it locks the cookie database) before running.
